#include<stdio.h>
#include<conio.h>
main()
{
//  int a=0;
//   scanf("%3d",&a);  //  scans only three characters

// All thee unformated funcions works with character and strings only   	

//char c;
//printf("enter the character");
//c=getchar();
//putchar(c);

//getche(); // shows the entered response
//getch(); // does not show the entered response

//char str[90];
//gets(str);
//puts(str);
}
